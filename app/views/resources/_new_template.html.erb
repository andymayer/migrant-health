<section class="section" aria-label="Content">
  <div class="strip strip--brand-color strip--resources">
    <div class="container">
      <div class="row">
        <div class="col-sm-8 strip__left">
          <%= render 'layouts/breadcrumb' %>
          <div class="jumbotron">
            <h1 class="title--strip">New <%= resource.class.name.titleize %></h1>
            <p class="intro">Create a new <%= resource.class.name.titleize %>.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="strip">
    <div class="container">
      <div class="row">
        <%= render 'form', resource: resource %>
      </div>
    </div>
  </div>
</section>
