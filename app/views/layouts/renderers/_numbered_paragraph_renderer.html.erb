<% if chunk.title %>
  <h2><%= chunk.title %></h2>
<% end %>

<% if chunk.intro %>
  <%= fancy_paragraphs(chunk.intro) %>
<% end %>

<ol class="big-ol">
  <% chunk.content.split("\n").each do |point| %>
    <li><%= point %></li>
  <% end %>
</ol>

<% if chunk.after %>
  <%= fancy_paragraphs(chunk.after) %>
<% end %>
