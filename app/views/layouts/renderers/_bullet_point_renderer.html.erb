<% if chunk.title %>
  <h2><%= chunk.title %></h2>
<% end %>

<% if chunk.intro %>
  <p><%= chunk.intro %></p>
<% end %>

<ul class="bullets">
  <% chunk.content.split("\n").each do |point| %>
    <li><%= point %></li>
  <% end %>
</ul>

<% if chunk.after %>
  <p><%= chunk.after %></p>
<% end %>
