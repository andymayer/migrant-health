$('#vote_down_answer_<%= @answer.id %>').bind('ajax:success', function(){
   $(this).parent().parent().find('#vote_count_answer_<%= @answer.id %>').html('<%= escape_javascript @answer.find_votes_for(:vote_scope => "rank").sum(:vote_weight).to_s %>');
});
