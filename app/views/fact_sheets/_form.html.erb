<%= form_with(model: fact_sheet, local: true) do |form| %>
  <% if fact_sheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fact_sheet.errors.count, "error") %> prohibited this fact_sheet from being saved:</h2>

      <ul>
      <% fact_sheet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
